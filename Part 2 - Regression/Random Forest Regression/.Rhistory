print("Hello world")
setwd("C:/Users/josue/Escritorio/MachineLearning/MachineLearning-For-DataScience/Part 2 - Regression/Random Forest Regression")
#importando dataset
dataset = read.csv('Position_Salaries.csv')
dataset = dataset[,2:3]
#Ajustar el modelo de regresion con el conjunto de datos
install.packages("randomForest")
library(randomForest)
#Prediccion de nuevos resultado con Random Forest Regression
y_pred = predict(regression, newdata=data.frame(Level = 6.5))
library(randomForest)
?randomForest
#Ajustar el modelo de regresion con el conjunto de datos
install.packages("randomForest")
library(randomForest)
install.packages("randomForest")
install.packages("randomForest")
set.seed(1234)
#dataframe = con []
#vector = con $
regression = randomForest(x = dataset[1],
y = dataset$Salary,
ntree = 500)
#Prediccion de nuevos resultado con Random Forest Regression
y_pred = predict(regression, newdata=data.frame(Level = 6.5))
#Visualizacion del modelo de Random Forest Regression
library(ggplot2)
x_grid = seq(min(dataset$Level),max(dataset$Level),0.1)
ggplot() +
geom_point(aes(x = dataset$Level, y = dataset$Salary),
colour = "red") +#colocando puntos
geom_line(aes(x = x_grid, y = predict(regression, newdata=data.frame(Level = x_grid))),
colour = "blue") + #Colocando recta de regresion
ggtitle("Prediccion (Random Forest Regression)")+
xlab("Nivel del empleado") +
ylab("Sueldo (en $)")
#dataframe = con []
#vector = con $
regression = randomForest(x = dataset[1],
y = dataset$Salary,
ntree = 500)
library(randomForest)
set.seed(1234)
#dataframe = con []
#vector = con $
regression = randomForest(x = dataset[1],
y = dataset$Salary,
ntree = 500)
#Prediccion de nuevos resultado con Random Forest Regression
y_pred = predict(regression, newdata=data.frame(Level = 6.5))
#Visualizacion del modelo de Random Forest Regression
library(ggplot2)
x_grid = seq(min(dataset$Level),max(dataset$Level),0.1)
ggplot() +
geom_point(aes(x = dataset$Level, y = dataset$Salary),
colour = "red") +#colocando puntos
geom_line(aes(x = x_grid, y = predict(regression, newdata=data.frame(Level = x_grid))),
colour = "blue") + #Colocando recta de regresion
ggtitle("Prediccion (Random Forest Regression)")+
xlab("Nivel del empleado") +
ylab("Sueldo (en $)")
#dataframe = con []
#vector = con $
regression = randomForest(x = dataset[1],
y = dataset$Salary,
ntree = 330)
#Prediccion de nuevos resultado con Random Forest Regression
y_pred = predict(regression, newdata=data.frame(Level = 6.5))
#Visualizacion del modelo de Random Forest Regression
library(ggplot2)
x_grid = seq(min(dataset$Level),max(dataset$Level),0.1)
ggplot() +
geom_point(aes(x = dataset$Level, y = dataset$Salary),
colour = "red") +#colocando puntos
geom_line(aes(x = x_grid, y = predict(regression, newdata=data.frame(Level = x_grid))),
colour = "blue") + #Colocando recta de regresion
ggtitle("Prediccion (Random Forest Regression)")+
xlab("Nivel del empleado") +
ylab("Sueldo (en $)")
#dataframe = con []
#vector = con $
regression = randomForest(x = dataset[1],
y = dataset$Salary,
ntree = 350)
#Prediccion de nuevos resultado con Random Forest Regression
y_pred = predict(regression, newdata=data.frame(Level = 6.5))
#Visualizacion del modelo de Random Forest Regression
library(ggplot2)
x_grid = seq(min(dataset$Level),max(dataset$Level),0.1)
ggplot() +
geom_point(aes(x = dataset$Level, y = dataset$Salary),
colour = "red") +#colocando puntos
geom_line(aes(x = x_grid, y = predict(regression, newdata=data.frame(Level = x_grid))),
colour = "blue") + #Colocando recta de regresion
ggtitle("Prediccion (Random Forest Regression)")+
xlab("Nivel del empleado") +
ylab("Sueldo (en $)")
#dataframe = con []
#vector = con $
regression = randomForest(x = dataset[1],
y = dataset$Salary,
ntree = 340)
#Prediccion de nuevos resultado con Random Forest Regression
y_pred = predict(regression, newdata=data.frame(Level = 6.5))
#Visualizacion del modelo de Random Forest Regression
library(ggplot2)
x_grid = seq(min(dataset$Level),max(dataset$Level),0.1)
ggplot() +
geom_point(aes(x = dataset$Level, y = dataset$Salary),
colour = "red") +#colocando puntos
geom_line(aes(x = x_grid, y = predict(regression, newdata=data.frame(Level = x_grid))),
colour = "blue") + #Colocando recta de regresion
ggtitle("Prediccion (Random Forest Regression)")+
xlab("Nivel del empleado") +
ylab("Sueldo (en $)")
#dataframe = con []
#vector = con $
regression = randomForest(x = dataset[1],
y = dataset$Salary,
ntree = 330)
#Prediccion de nuevos resultado con Random Forest Regression
y_pred = predict(regression, newdata=data.frame(Level = 6.5))
#Visualizacion del modelo de Random Forest Regression
library(ggplot2)
x_grid = seq(min(dataset$Level),max(dataset$Level),0.1)
ggplot() +
geom_point(aes(x = dataset$Level, y = dataset$Salary),
colour = "red") +#colocando puntos
geom_line(aes(x = x_grid, y = predict(regression, newdata=data.frame(Level = x_grid))),
colour = "blue") + #Colocando recta de regresion
ggtitle("Prediccion (Random Forest Regression)")+
xlab("Nivel del empleado") +
ylab("Sueldo (en $)")
#dataframe = con []
#vector = con $
regression = randomForest(x = dataset[1],
y = dataset$Salary,
ntree = 400)
#Prediccion de nuevos resultado con Random Forest Regression
y_pred = predict(regression, newdata=data.frame(Level = 6.5))
#Visualizacion del modelo de Random Forest Regression
library(ggplot2)
x_grid = seq(min(dataset$Level),max(dataset$Level),0.1)
ggplot() +
geom_point(aes(x = dataset$Level, y = dataset$Salary),
colour = "red") +#colocando puntos
geom_line(aes(x = x_grid, y = predict(regression, newdata=data.frame(Level = x_grid))),
colour = "blue") + #Colocando recta de regresion
ggtitle("Prediccion (Random Forest Regression)")+
xlab("Nivel del empleado") +
ylab("Sueldo (en $)")
#dataframe = con []
#vector = con $
regression = randomForest(x = dataset[1],
y = dataset$Salary,
ntree = 500)
#Prediccion de nuevos resultado con Random Forest Regression
y_pred = predict(regression, newdata=data.frame(Level = 6.5))
#Visualizacion del modelo de Random Forest Regression
library(ggplot2)
x_grid = seq(min(dataset$Level),max(dataset$Level),0.1)
ggplot() +
geom_point(aes(x = dataset$Level, y = dataset$Salary),
colour = "red") +#colocando puntos
geom_line(aes(x = x_grid, y = predict(regression, newdata=data.frame(Level = x_grid))),
colour = "blue") + #Colocando recta de regresion
ggtitle("Prediccion (Random Forest Regression)")+
xlab("Nivel del empleado") +
ylab("Sueldo (en $)")
#dataframe = con []
#vector = con $
regression = randomForest(x = dataset[1],
y = dataset$Salary,
ntree = 450)
#Prediccion de nuevos resultado con Random Forest Regression
y_pred = predict(regression, newdata=data.frame(Level = 6.5))
#Visualizacion del modelo de Random Forest Regression
library(ggplot2)
x_grid = seq(min(dataset$Level),max(dataset$Level),0.1)
ggplot() +
geom_point(aes(x = dataset$Level, y = dataset$Salary),
colour = "red") +#colocando puntos
geom_line(aes(x = x_grid, y = predict(regression, newdata=data.frame(Level = x_grid))),
colour = "blue") + #Colocando recta de regresion
ggtitle("Prediccion (Random Forest Regression)")+
xlab("Nivel del empleado") +
ylab("Sueldo (en $)")
#dataframe = con []
#vector = con $
regression = randomForest(x = dataset[1],
y = dataset$Salary,
ntree = 475)
#Prediccion de nuevos resultado con Random Forest Regression
y_pred = predict(regression, newdata=data.frame(Level = 6.5))
#Visualizacion del modelo de Random Forest Regression
library(ggplot2)
x_grid = seq(min(dataset$Level),max(dataset$Level),0.1)
ggplot() +
geom_point(aes(x = dataset$Level, y = dataset$Salary),
colour = "red") +#colocando puntos
geom_line(aes(x = x_grid, y = predict(regression, newdata=data.frame(Level = x_grid))),
colour = "blue") + #Colocando recta de regresion
ggtitle("Prediccion (Random Forest Regression)")+
xlab("Nivel del empleado") +
ylab("Sueldo (en $)")
#dataframe = con []
#vector = con $
regression = randomForest(x = dataset[1],
y = dataset$Salary,
ntree = 450)
#Prediccion de nuevos resultado con Random Forest Regression
y_pred = predict(regression, newdata=data.frame(Level = 6.5))
#Visualizacion del modelo de Random Forest Regression
library(ggplot2)
x_grid = seq(min(dataset$Level),max(dataset$Level),0.1)
ggplot() +
geom_point(aes(x = dataset$Level, y = dataset$Salary),
colour = "red") +#colocando puntos
geom_line(aes(x = x_grid, y = predict(regression, newdata=data.frame(Level = x_grid))),
colour = "blue") + #Colocando recta de regresion
ggtitle("Prediccion (Random Forest Regression)")+
xlab("Nivel del empleado") +
ylab("Sueldo (en $)")
#dataframe = con []
#vector = con $
regression = randomForest(x = dataset[1],
y = dataset$Salary,
ntree = 550)
#Prediccion de nuevos resultado con Random Forest Regression
y_pred = predict(regression, newdata=data.frame(Level = 6.5))
#Visualizacion del modelo de Random Forest Regression
library(ggplot2)
x_grid = seq(min(dataset$Level),max(dataset$Level),0.1)
ggplot() +
geom_point(aes(x = dataset$Level, y = dataset$Salary),
colour = "red") +#colocando puntos
geom_line(aes(x = x_grid, y = predict(regression, newdata=data.frame(Level = x_grid))),
colour = "blue") + #Colocando recta de regresion
ggtitle("Prediccion (Random Forest Regression)")+
xlab("Nivel del empleado") +
ylab("Sueldo (en $)")
#Visualizacion del modelo de Random Forest Regression
library(ggplot2)
x_grid = seq(min(dataset$Level),max(dataset$Level),0.01)
ggplot() +
geom_point(aes(x = dataset$Level, y = dataset$Salary),
colour = "red") +#colocando puntos
geom_line(aes(x = x_grid, y = predict(regression, newdata=data.frame(Level = x_grid))),
colour = "blue") + #Colocando recta de regresion
ggtitle("Prediccion (Random Forest Regression)")+
xlab("Nivel del empleado") +
ylab("Sueldo (en $)")
